name: Market refresh (BRAPI Pro)

# Mantém o market.* fresco APÓS o bootstrap: refresh diário leve (preços +
# dividendos + indicadores spot) e refresh anual semanal (demonstrações
# completas). Opera só sobre os tickers JÁ carregados (--source market), sem
# gastar cota com pendentes (esses ficam no bootstrap-brapi.yml).
on:
  workflow_dispatch:
    inputs:
      mode:
        description: "O que rodar"
        type: choice
        options: ["daily", "annual", "both"]
        default: "daily"
  schedule:
    - cron: "0 9 * * *"    # diário 06:00 BRT — daily (após o bootstrap das 03:00)
    - cron: "0 8 * * 6"    # sábado 05:00 BRT — annual (demonstrações)

permissions:
  contents: read

concurrency:
  group: market-refresh
  cancel-in-progress: false

jobs:
  refresh:
    name: Market refresh
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      APP_ENV: production
      PYTHONUNBUFFERED: "1"
      SUPABASE_UNIFICADO_URL: ${{ secrets.SUPABASE_UNIFICADO_URL }}
      DATABASE_URL:           ${{ secrets.DATABASE_URL }}
      SUPABASE_DB_URL:        ${{ secrets.SUPABASE_DB_URL }}
      OWNER_USER_ID:          ${{ secrets.OWNER_USER_ID }}
      BRAPI_TOKEN:            ${{ secrets.BRAPI_TOKEN }}
      MARKET_DELAY:           "1.0"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-pipeline.txt
      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-pipeline.txt
      - name: Confirmar token (sem expor)
        run: |
          python - <<'PY'
          import core.brapi as b
          assert b._token(), "BRAPI_TOKEN ausente"
          print("BRAPI_TOKEN ok | len:", len(b._token()))
          PY
      # Decide o modo: schedule de sábado 08:00 UTC => annual; demais => daily.
      # workflow_dispatch usa o input 'mode'.
      - name: Definir modo
        id: mode
        run: |
          M="${{ github.event.inputs.mode }}"
          if [ -z "$M" ]; then
            if [ "${{ github.event.schedule }}" = "0 8 * * 6" ]; then M="annual"; else M="daily"; fi
          fi
          echo "mode=$M" >> "$GITHUB_OUTPUT"
          echo "Modo: $M"
      - name: Daily (preços + dividendos + spot)
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py daily --source market --json
      - name: Annual (demonstrações)
        if: steps.mode.outputs.mode == 'annual' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py annual --source market --json
      - name: Reprocessar indicadores (sem rede)
        if: always()
        run: python run_market_ingest.py reprocess --json
      - name: FIIs (ranking — universo type=fund, filtra ETF por setor)
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis --json
      - name: FIIs — enriquecimento CVM (segmento/tipo/VPA/cotistas via CNPJ)
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-cvm --json
      - name: FIIs — cobertura CVM (mensal, trimestral, anual, DFIN e eventuais)
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-cvm-structured --years 2 --json
      - name: FIIs — endpoints dedicados Brapi v2 (métricas e look-through)
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-v2 --json
      - name: FIIs — histórico diário Brapi v2 (backfill incremental)
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-v2-history --years 20 --json
      - name: FIIs — look-through de CRIs pela CVM
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-cvm-cri --years 5 --json
      - name: FIIs — documentos públicos prioritários
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-documents --limit 25 --json
      - name: FIIs — cadastro CVM com fundos cancelados
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-registry --json
      - name: FIIs — security master histórico oficial B3
        if: steps.mode.outputs.mode == 'annual' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-b3-history --years 15 --json
      - name: FIIs — resolução de locatários, devedores e emissores
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-entities --json
      - name: FIIs — calibração dos parsers por revisão humana
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-confidence --json
      - name: FIIs — reconstrução PIT e backtest walk-forward
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-pit-backtest --years 10 --json
      - name: FIIs — monitoramento e gates
        if: always()
        run: python run_market_ingest.py fiis-monitor --json
      - name: FIIs — séries históricas (preços + rendimentos p/ backtest)
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py fiis-series --json
      - name: Benchmark FIIs (IFIX via ETF XFIX11 p/ backtest)
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py benchmark --json
      - name: Setores — taxonomia B3 (public.setores + mapa aprendido CVM->B3)
        if: steps.mode.outputs.mode == 'daily' || steps.mode.outputs.mode == 'both'
        run: python run_market_ingest.py setores --json
