# Dependências mínimas para o pipeline headless (GitHub Actions / CLI)
# Streamlit é necessário porque core.database usa st.cache_resource/st.cache_data.
streamlit>=1.39.0,<2.0.0
sqlalchemy>=2.0.0,<3.0.0
psycopg2-binary>=2.9.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
pandas>=2.2.0,<3.0.0
yfinance>=0.2.50,<0.3.0
requests>=2.32.0,<3.0.0
# Scraping/parsing do saneamento (Fundamentus/Status Invest usam bs4) e extração
# de texto dos documentos CVM (pdfplumber). Sem eles, as fontes web voltam vazias
# no Actions -> Fontes=0 -> nenhuma correcao e aplicada. (bs4 usa html.parser da
# stdlib, entao lxml nao e necessario.)
beautifulsoup4>=4.12.0,<5.0.0
pdfplumber>=0.11.0,<1.0.0
pypdf>=4.0.0,<6.0.0  # extrator primário (rápido) de texto CVM
